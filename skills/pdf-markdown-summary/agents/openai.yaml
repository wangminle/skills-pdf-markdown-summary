interface:
  display_name: "PDF Markdown Summary"
  short_description: "Convert PDFs to Markdown with figures/tables"
  default_prompt: "Use $pdf-markdown-summary to convert this PDF to Markdown, extract figures and tables, and prepare a figure-aware summary."
